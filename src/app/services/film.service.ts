import { inject, Injectable } from '@angular/core';

// comment here
import {
   doc,
   docData,
   DocumentReference,
   getDoc,
   updateDoc,
   collection,
   addDoc,
   deleteDoc,
   collectionData,
   Timestamp,
} from '@angular/fire/firestore';
